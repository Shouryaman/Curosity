{
  "name": "engineering_team",
  "private": true,
  "scripts": {
    "api": "cross-env PYTHONUTF8=1 PYTHONIOENCODING=utf-8 PYTHONPATH=src python -m uvicorn engineering_team.api.main:app --reload --reload-dir src --host 0.0.0.0 --port 8000",
    "stack": "concurrently -n api,web -c blue,magenta \"npm run api\" \"npm run dev\"",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3"
  }
}
